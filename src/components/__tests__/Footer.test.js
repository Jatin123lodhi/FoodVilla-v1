import {Footer} from "../Footer";
import { render } from "@testing-library/react";
test("footer should be there on loading page",()=>{
    const footer = render(<Footer/>);
    // console.log(footer,' >~~footer<');
    expect().toBe();
});